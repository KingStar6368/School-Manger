@* EmptyTablePage.cshtml *@
@{
    ViewData["Title"] = "صفحه جدول";
    Layout = "_Layout";
}

<div class="container-fluid bg-dark text-white" dir="rtl">
    <!-- Header Section -->
    <div class="row py-4 bg-black">
        <div class="col-12 text-center">
            <h3>عنوان صفحه جدول</h3>
        </div>
    </div>

    <!-- Table Section -->
    <div class="row mt-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-info">
                    <div class="d-flex justify-content-between align-items-center">
                        <h4 class="mb-0">عنوان جدول</h4>
                        <button class="btn btn-success">
                            <i class="bi bi-plus-circle"></i> افزودن جدید
                        </button>
                    </div>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-dark table-hover">
                            <thead>
                                <tr>
                                    <th>ردیف</th>
                                    <th>ستون اول</th>
                                    <th>ستون دوم</th>
                                    <th>عملیات</th>
                                </tr>
                            </thead>
                            <tbody>
                                <!-- Table rows will go here -->
                                <tr>
                                    <td>1</td>
                                    <td>مقدار نمونه</td>
                                    <td>مقدار نمونه</td>
                                    <td>
                                        <button class="btn btn-sm btn-warning">
                                            <i class="bi bi-pencil"></i> ویرایش
                                        </button>
                                        <button class="btn btn-sm btn-danger">
                                            <i class="bi bi-trash"></i> حذف
                                        </button>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            // Table related JavaScript goes here
        });
    </script>
}